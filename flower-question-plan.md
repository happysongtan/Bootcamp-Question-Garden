# 꽃피는 질문 리브랜딩 기획

## 1. 핵심 방향

### 서비스 이름

꽃피는 질문

### 한 줄 소개

반 친구들이 익명 질문, 공감, 답변을 쌓아가며 각 반의 대표 꽃을 함께 피우는 질문 공유 서비스.

### 핵심 컨셉

기존 서비스는 익명 질문과 공감으로 공통 막힘을 모으는 질문 게시판이었다.  
새 방향은 여기에 "반별 꽃 성장"을 더해 질문 참여를 긍정적인 행동으로 바꾼다.

```text
질문이 쌓이면 우리 반 꽃이 자란다.
답변이 달리고 해결되면 꽃이 더 활짝 핀다.
```

### 바꾸려는 감정

- 기존: 질문 게시판에 글을 올린다.
- 변경: 우리 반 꽃에 물을 주듯 질문을 남긴다.
- 기존: 모르는 것을 드러내는 행동이라 부담스럽다.
- 변경: 반 전체 학습에 기여하는 행동이라 가볍게 참여할 수 있다.

---

## 2. 현재 프로젝트에서 아쉬운 점

### 이름

`질문온도계`는 공감 수가 높을수록 뜨겁다는 의미는 있지만, 사용자가 감정적으로 붙잡을 만한 이미지가 약하다.  
`꽃피는 질문`은 질문이 쌓이고 성장한다는 이미지를 바로 전달한다.

### 색감

현재 UI는 흰색, 파랑, 초록 중심이라 평범한 관리자 페이지 느낌이 강하다.  
꽃과 반별 성장 컨셉을 살리려면 더 따뜻하고 산뜻한 색감이 필요하다.

### 상세 화면 구분감

질문 상세를 열었을 때 질문 본문과 답변 영역이 비슷한 카드 안에 이어져 보여서 구분이 약하다.  
사용자는 "지금 보는 내용이 질문인지 답변인지" 한눈에 파악해야 한다.

### 분류 구조

현재 카테고리는 `Java`, `Spring`, `DB` 같은 기술 주제 중심이다.  
반별 사용을 고려하면 "반"과 "기술 카테고리"를 분리하는 것이 좋다.

---

## 3. 반별 꽃 시스템

### 기본 원칙

- 꽃은 질문마다 여러 개 키우지 않는다.
- 각 반마다 대표 꽃 하나만 키운다.
- 각 반은 원하는 꽃 종류를 선택할 수 있다.
- 질문 활동이 누적될수록 그 반의 꽃이 성장한다.
- 꽃 성장은 개인 경쟁이 아니라 반 전체의 공동 결과로 보여준다.

### 반과 꽃의 관계

```text
백엔드 1반 -> 해바라기
백엔드 2반 -> 튤립
프론트엔드 1반 -> 벚꽃
알고리즘 스터디반 -> 민들레
```

### 화면에서 보여줄 문장 예시

```text
백엔드 3반의 해바라기가 꽃봉오리 단계까지 자랐어요.
질문 12개, 공감 31개, 답변 8개가 모였어요.
```

---

## 4. 꽃 종류 후보

처음부터 너무 많이 제공하지 않고 4-6개 정도로 시작하는 것이 좋다.

| 꽃 | 분위기 | 추천 사용처 |
| --- | --- | --- |
| 해바라기 | 밝고 활발함 | 질문과 공감이 많은 반 |
| 튤립 | 단정하고 산뜻함 | 기본 선택지 |
| 벚꽃 | 부드럽고 따뜻함 | 감성적인 분위기 |
| 민들레 | 끈기와 성장 | 초보자 중심 반 |
| 라벤더 | 차분함 | 조용하지만 꾸준한 반 |
| 코스모스 | 자유로움 | 프로젝트반, 스터디반 |

### 1차 추천

- 해바라기
- 튤립
- 벚꽃
- 민들레

이 네 개만 있어도 반마다 고르는 재미가 생기고, 디자인 부담도 과하지 않다.

---

## 5. 성장 단계

꽃은 하나지만 단계별로 모습이 바뀐다.

| 단계 | 이름 | 조건 예시 | 화면 느낌 |
| --- | --- | --- | --- |
| 0 | 씨앗 | 0점 | 흙 위에 씨앗 |
| 1 | 새싹 | 10점 이상 | 작은 잎 |
| 2 | 줄기 | 25점 이상 | 줄기와 잎 |
| 3 | 꽃봉오리 | 50점 이상 | 닫힌 꽃봉오리 |
| 4 | 개화 | 80점 이상 | 꽃이 핀 상태 |
| 5 | 만개 | 120점 이상 | 풍성하게 핀 꽃 |

### 성장 단계 문구

```text
씨앗 단계: 아직 첫 질문을 기다리고 있어요.
새싹 단계: 우리 반 질문이 싹트기 시작했어요.
줄기 단계: 질문과 답변이 꾸준히 쌓이고 있어요.
꽃봉오리 단계: 곧 꽃이 필 만큼 활발해졌어요.
개화 단계: 우리 반 질문 문화가 피어났어요.
만개 단계: 질문과 답변이 풍성하게 모였어요.
```

---

## 6. 성장 점수 계산

### 기본 점수안

| 활동 | 점수 |
| --- | ---: |
| 질문 등록 | +3 |
| 공감 추가 | +1 |
| 답변 등록 | +4 |
| 해결 완료 | +5 |

### 계산 예시

```text
질문 10개 = 30점
공감 20개 = 20점
답변 6개 = 24점
해결 완료 3개 = 15점
총점 = 89점
성장 단계 = 개화
```

### 이 기준의 장점

- 질문만 많이 올리는 것보다 답변과 해결까지 중요하게 만든다.
- 공감은 가볍게 누를 수 있으므로 점수를 낮게 둔다.
- 답변과 해결은 학습 완성도에 가까우므로 점수를 높게 둔다.

### 조정 가능성

초반에 꽃이 너무 늦게 자라면 기준을 낮춘다.  
반대로 하루 만에 만개하면 단계 기준을 높인다.

---

## 7. 반 분류 설계

### 추천 구조

반과 기술 카테고리를 분리한다.

```text
반: 백엔드 1반, 백엔드 2반, 프론트엔드 1반
기술 카테고리: Java, Spring, DB, Git, Algorithm
```

### 왜 분리하는가

카테고리를 반으로 바꿔버리면 `Spring 질문만 보기` 같은 기능이 사라진다.  
반은 질문이 속한 공간이고, 카테고리는 질문의 주제다.

### 질문 데이터에 추가될 후보 필드

```text
className 또는 classId
```

간단한 MVP라면 문자열로 시작할 수 있다.

```text
class_name VARCHAR(50)
```

나중에 반 설정과 꽃 선택을 확장하려면 별도 테이블이 낫다.

```text
classes
- id
- name
- flower_type
- created_at
```

---

## 8. 화면 구성 변경안

### 메인 화면

상단에서 반을 먼저 선택한다.

```text
꽃피는 질문
반 친구들과 질문을 모아 우리 반 꽃을 피워요.

[백엔드 1반 v]

백엔드 1반의 해바라기
성장 단계: 꽃봉오리
성장도: 68%
질문 12개 · 공감 31개 · 답변 8개 · 해결 5개
```

그 아래 기존 질문 목록을 보여준다.

```text
검색어 입력
카테고리 선택
정렬 선택

질문 목록
질문 카드
질문 카드
질문 카드
```

### 질문 카드

질문 카드에는 반 정보보다 기술 카테고리와 상태가 중요하다.  
이미 반을 선택한 화면이기 때문이다.

```text
[Spring] [답변 대기]
JdbcTemplate이 정확히 뭔지 모르겠습니다.
Repository에서 JdbcTemplate을 사용하는 이유가 궁금합니다.
#Spring #JDBC
궁금해요 15 · 답변 2 · 조회 5
```

### 상세 화면

질문 본문과 답변을 명확하게 나눈다.

```text
질문
제목
본문
작성자, 작성일, 카테고리, 태그
나도 궁금해요

답변
답변 카드 1
답변 카드 2

답변 작성
입력 폼
```

---

## 9. 상세 화면 구분감 개선

### 질문 본문 영역

- 답변보다 더 큰 여백을 준다.
- 배경을 아주 연한 꽃잎색 또는 연한 초록색으로 둔다.
- 왼쪽에 `질문` 라벨을 붙인다.
- 질문 내용은 `white-space: pre-wrap`로 줄바꿈을 살린다.

### 답변 영역

- 질문 본문 아래에 명확한 섹션 제목을 둔다.
- 답변 카드는 흰색 또는 아주 연한 회색 배경으로 둔다.
- 답변 사이에는 간격을 충분히 둔다.
- 답변 작성 폼은 답변 목록과 분리한다.

### 상태별 표현

| 상태 | 기존 표현 | 변경 표현 |
| --- | --- | --- |
| WAITING | 답변 대기 | 새싹 대기 |
| ANSWERED | 답변 있음 | 꽃봉오리 |
| SOLVED | 해결 완료 | 활짝 핀 질문 |

단, 상태 이름을 너무 은유적으로 바꾸면 기능 이해가 어려울 수 있다.  
화면에는 `답변 대기`, `답변 있음`, `해결 완료`를 유지하고, 보조 아이콘이나 색으로 꽃 컨셉을 주는 방식이 더 안전하다.

---

## 10. 색감 방향

### 현재 문제

현재 색감은 파랑 버튼과 흰 카드 중심이라 무난하지만, 꽃 성장 컨셉과는 거리가 있다.

### 새 팔레트 후보

| 역할 | 색상 | 용도 |
| --- | --- | --- |
| 배경 | `#F7FBF3` | 연한 정원 배경 |
| 카드 | `#FFFFFF` | 질문 카드 |
| 메인 초록 | `#2F7D4E` | 주요 버튼, 강조 |
| 잎 초록 | `#8FBC8F` | 보조 요소 |
| 꽃잎 핑크 | `#F4A7B9` | 포인트 |
| 꽃잎 노랑 | `#F8D66D` | 성장도, 하이라이트 |
| 흙색 | `#8B6F47` | 씨앗/줄기 보조 |
| 본문 글자 | `#263126` | 기본 텍스트 |
| 보조 글자 | `#66736A` | 메타 정보 |

### 디자인 톤

- 귀엽지만 유치하지 않게
- 꽃은 크게 하나만 보여주고, 나머지 UI는 깔끔하게
- 배경 장식은 적게 사용
- 정보가 먼저 읽히고 꽃은 동기를 주는 역할로 둔다

---

## 11. 데이터 구조 변경 후보

### 간단한 구현

기존 `questions` 테이블에 반 이름을 바로 추가한다.

```sql
ALTER TABLE questions ADD COLUMN class_name VARCHAR(50) NOT NULL DEFAULT '백엔드 1반';
```

꽃 정보는 코드나 프론트 상수로 관리한다.

```javascript
const classFlowers = {
  "백엔드 1반": "sunflower",
  "백엔드 2반": "tulip"
};
```

### 확장 가능한 구현

반 테이블을 별도로 만든다.

```sql
CREATE TABLE classes (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    flower_type VARCHAR(50) NOT NULL,
    created_at DATETIME NOT NULL
);

ALTER TABLE questions ADD COLUMN class_id BIGINT;
```

### 추천

현재 프로젝트 규모에서는 먼저 `class_name` 문자열 방식으로 시작하는 것이 좋다.  
발표 전 MVP라면 데이터 구조를 단순하게 가져가고, 꽃 종류도 고정 목록으로 처리하는 편이 안정적이다.

---

## 12. API 변경 후보

### 질문 목록 조회

현재:

```http
GET /api/questions?category=Spring&sort=likes
```

변경 후보:

```http
GET /api/questions?className=백엔드1반&category=Spring&sort=likes
```

### 질문 등록

요청에 `className` 추가:

```json
{
  "className": "백엔드 1반",
  "title": "JdbcTemplate이 정확히 뭔지 모르겠습니다.",
  "content": "Repository에서 JdbcTemplate을 사용하는 이유가 궁금합니다.",
  "category": "Spring",
  "nickname": "익명의 감자",
  "password": "1234",
  "tags": ["Spring", "JDBC"]
}
```

### 반 성장 정보 조회

새 API 후보:

```http
GET /api/classes/{className}/flower
```

응답 후보:

```json
{
  "className": "백엔드 1반",
  "flowerType": "sunflower",
  "growthScore": 89,
  "growthStage": "BLOOMED",
  "questionCount": 10,
  "likeCount": 20,
  "answerCount": 6,
  "solvedCount": 3
}
```

---

## 13. 구현 우선순위

### 0단계: 꽃 제외 즉시 반영할 변경사항

꽃 디자인 고도화와 꽃 종류 변경 기능은 별도 작업으로 남겨두고, 먼저 서비스 사용 흐름을 정리하는 변경부터 반영한다.

#### 반 목록 변경

현재 임시 반 목록은 실제 사용 맥락과 맞지 않는다.

기존 후보:

```text
백엔드 1반
백엔드 2반
프론트엔드 1반
알고리즘 스터디반
```

변경할 반 목록:

```text
21반
22반
23반
24반
25반
26반
27반
28반
29반
```

적용 위치:

- 메인 화면 반 선택 드롭다운
- 질문 등록 모달의 반 선택 드롭다운
- 백엔드 기본 반 이름
- 샘플 데이터
- 반별 질문 필터
- 반별 인기 질문 조회
- 반별 성장 정보 조회

기본 반은 `21반`으로 둔다.

#### 공감 취소 기능

현재 공감은 한 번 누르면 localStorage에 저장되어 다시 누를 수 없게 막는 방식이다.  
사용자 기대에 맞게 다시 누르면 공감이 취소되도록 바꾼다.

동작 규칙:

```text
공감하지 않은 질문 클릭 -> 공감 수 +1, localStorage에 질문 ID 저장
이미 공감한 질문 클릭 -> 공감 수 -1, localStorage에서 질문 ID 제거
```

백엔드 API 후보:

```http
POST /api/questions/{questionId}/like
DELETE /api/questions/{questionId}/like
```

`POST`는 공감 추가, `DELETE`는 공감 취소로 역할을 나눈다.

프론트 변경:

- 버튼 문구를 상태에 따라 변경한다.
- 공감 전: `나도 궁금해요`
- 공감 후: `공감 취소`
- 취소 후 질문 목록, 인기 질문, 꽃 성장 정보를 다시 불러온다.

주의할 점:

- 서버는 최소 0 미만으로 공감 수가 내려가지 않게 막아야 한다.
- localStorage가 서버의 진짜 사용자 인증은 아니므로, MVP 수준의 중복 방지라는 점을 유지한다.

#### 질문 카드 클릭으로 상세 열기

현재는 `상세 보기` 버튼을 눌러야 상세 화면이 열린다.  
게시판 사용감 개선을 위해 질문 카드 자체를 클릭해도 상세 화면이 열리게 한다.

동작 규칙:

- 질문 카드 클릭 -> 상세 모달 열기
- `상세 보기` 버튼 클릭 -> 상세 모달 열기
- 공감 버튼 클릭 -> 공감 추가/취소만 실행

프론트 구현 주의:

- 질문 카드에 `data-detail-id`를 부여한다.
- 카드 클릭 이벤트에서 `openDetail(question.id)`를 호출한다.
- 공감 버튼 클릭 시 `event.stopPropagation()`으로 카드 클릭 이벤트가 같이 실행되지 않게 한다.
- 키보드 접근성을 위해 카드에 `tabindex="0"`과 Enter/Space 처리도 고려한다.

#### 질문 등록 후 선택 반 동기화

현재 메인에서 보고 있는 반과 질문 등록 모달에서 선택한 반이 다를 수 있다.  
다른 반으로 질문을 등록한 뒤에도 메인 선택 반이 그대로면 방금 등록한 글이 목록에 보이지 않아 혼란스럽다.

변경 규칙:

```text
질문 등록 시 선택한 반으로 메인 반 선택값을 변경한다.
그 반 기준으로 질문 목록, 인기 질문, 성장 정보를 새로 불러온다.
```

예시:

```text
현재 화면: 21반
등록 모달에서 선택한 반: 25반
등록 완료 후 화면: 25반 질문 목록
```

#### 이번 변경에서 제외하는 것

아래 항목은 의도적으로 이번 정리 범위에서 제외한다.

- 꽃 디자인 전면 개선
- 꽃 종류 변경 UI
- 꽃 종류 저장 API
- 꽃 이미지/SVG 에셋 교체
- 관리자 권한 또는 반장 권한

---

### 1단계: 리브랜딩

- 화면 제목을 `꽃피는 질문`으로 변경
- README와 문서 이름 정리
- 색감 변경
- 상세 화면 구분감 개선

### 2단계: 반 선택

- 고정 반 목록 추가
- 질문 등록 시 반 선택
- 질문 목록을 반별로 필터링
- 인기 질문도 반별로 필터링

### 3단계: 꽃 성장 표시

- 반별 성장 점수 계산
- 성장 단계 표시
- 대표 꽃 종류 표시
- 성장도 바 또는 단계 문구 표시

### 4단계: 꽃 종류 선택

- 반별 꽃 종류 고정 매핑
- 나중에 선택 기능으로 확장

### 5단계: 고도화

- 반 생성 기능
- 꽃 선택 기능
- 성장 히스토리
- 반별 비교 화면

---

## 14. 발표 문구

### 문제 제기

부트캠프에서는 같은 내용을 어려워하는 학생이 많아도 질문하기 부끄러워 혼자 넘기는 경우가 많다.

### 해결 방법

꽃피는 질문은 익명 질문과 공감, 답변을 통해 반 전체가 어디서 막히는지 보여주고, 질문 활동이 쌓일수록 각 반의 꽃이 자라도록 만든 서비스다.

### 차별점

질문을 단순히 게시글로 남기는 것이 아니라, 우리 반 꽃을 함께 키우는 경험으로 바꿨다.

### 한 줄 소개

```text
꽃피는 질문은 질문할수록 우리 반 꽃이 자라는 익명 질문 공유 서비스입니다.
```

---

## 15. 결정 사항 기록

- 2026-05-07: 서비스 이름을 `꽃피는 질문`으로 결정.
- 2026-05-07: 질문마다 꽃을 키우는 방식이 아니라, 각 반마다 대표 꽃 하나를 키우는 방식으로 결정.
- 2026-05-07: 반마다 원하는 꽃 종류를 선택할 수 있는 방향으로 확장 예정.
- 2026-05-07: 반 구분과 기술 카테고리는 분리해서 유지하는 방향으로 결정.
- 2026-05-07: 꽃 관련 고도화는 제외하고, 먼저 반 목록을 21반-29반으로 바꾸는 방향으로 결정.
- 2026-05-07: 공감 버튼은 다시 누르면 취소되는 토글 방식으로 변경 예정.
- 2026-05-07: 질문 카드는 `상세 보기` 버튼뿐 아니라 카드 클릭으로도 상세 화면에 들어가게 변경 예정.
